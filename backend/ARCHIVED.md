# Express Backend Archived

This Express + MongoDB backend has been archived.

The project now uses:

React (Frontend)
↓

WooCommerce REST API
↓

WordPress
↓

MySQL

The backend is preserved so it can be reused if future business requirements require a custom API.